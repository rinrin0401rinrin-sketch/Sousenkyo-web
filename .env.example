# Browser-safe variables exposed to Vite client code.
# Only put values here that are safe to ship to the browser.
VITE_PUBLIC_BASE_URL=

# Server/importer-only secrets.
# Do not prefix secrets with VITE_; keep real values in local .env files only.
IMPORTER_API_KEY=
IMPORTER_API_SECRET=
